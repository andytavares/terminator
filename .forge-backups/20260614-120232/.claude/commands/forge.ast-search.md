---
description: Structural code search using ast-grep (or semgrep fallback). Finds code by shape — call sites, patterns, usage — not by string match. Run the ast-search skill for: $ARGUMENTS
---

Run the `ast-search` skill for: $ARGUMENTS

Return `file:line` matches grouped by file, with a one-sentence summary of total match count and languages searched.
