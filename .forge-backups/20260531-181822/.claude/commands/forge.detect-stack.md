---
description: Detect languages, build tools, test runners, linters in this repo and write .claude/stack.json
---

Run the `build-detective` subagent. Have it walk the repository, identify every language, build system, test runner, linter, and CI configuration, and write the result to `.claude/stack.json`. Report a brief summary back to me — counts only, not the full file dump.
