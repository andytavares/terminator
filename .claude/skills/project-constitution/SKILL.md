---
name: project-constitution
description: Interactive, LLM-assisted authoring of .forge/constitution.md — the project soul file
---

# Project Constitution Skill

## Purpose

Create or update `.forge/constitution.md`, a soul file that encodes top-level non-negotiables, architectural principles, and project identity. Once written, this file is injected into Claude's context at every session start and after every context compaction.

## Constitution schema

`.forge/constitution.md` must contain exactly these six H2 sections (in order):

```
# <Project Name> — Project Constitution
<!-- Generated by /forge-constitution. Edit freely. Loaded every session. -->

## Purpose
## Non-negotiables
## Architectural principles
## Risk posture
## Team conventions
## Out of scope
```

Any section body may be `(none)` if intentionally empty, but the heading must always be present.

## How

### Create flow (no existing constitution)

1. **Scan the repo for signals.** Run the `researcher` subagent with this prompt:

   > "Scan this repo for implicit project principles. Read: README.md, CLAUDE.md, any files in docs/ matching _ADR_ or _CONTRIBUTING_, and .forge/\*/spec.md files. For each of the six constitution sections (Purpose, Non-negotiables, Architectural principles, Risk posture, Team conventions, Out of scope) — draft one concise entry. Return the six drafts and nothing else."

2. **Present each section one at a time.** For each section in schema order:

   - Show the section heading and the researcher's draft.
   - Ask: **"Accept, Edit, or Skip? [A/e/s]"**
   - Accept (default): use draft as-is.
   - Edit: prompt "Enter replacement text:" and accept free-form input.
   - Skip: set body to `(none)`.

3. **Assemble the file.** After all six sections, render the complete `.forge/constitution.md` content and display it to the user.

4. **Confirm before writing.** Ask: **"Write this to .forge/constitution.md? [Y/n]"**

   - Yes (default): write the file. Report the full path.
   - No: discard and exit without writing.

5. **Post-write reminder.** Output: "Commit `.forge/constitution.md` — it is checked in and loaded every session."

### Update flow (constitution already exists)

1. Display the current contents of `.forge/constitution.md`.
2. Ask: **"Update a section, regenerate from scratch, or cancel? [u/r/c]"**
   - Update a section: ask "Which section? (Purpose / Non-negotiables / Architectural principles / Risk posture / Team conventions / Out of scope)" → accept new text → rewrite only that H2 block in the file → confirm before writing.
   - Regenerate from scratch: run the full create flow (steps 1–5 above).
   - Cancel: exit with no changes.

## Invariants

- Never write the file without explicit user confirmation.
- Never omit a section heading — missing headings cause the session-start hook to warn.
- Never batch multiple sections in a single question — present them one at a time.
- The H1 title must be `# <Project Name> — Project Constitution` where `<Project Name>` is inferred from the repo name (`basename $(git rev-parse --show-toplevel)`).
