---
description: Ask any question about the codebase (technical or not) — routes to the codebase-oracle
---

Hand the following question to the `codebase-oracle` subagent and return its answer verbatim:

$ARGUMENTS

The oracle will classify the question, gather evidence, and answer in plain language with file:line citations. It will never edit files.
