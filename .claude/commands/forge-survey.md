---
description: Survey how a concern (auth, logging, error handling, retries, etc.) is implemented across the codebase
---

Run the `codebase-oracle` subagent with the `pattern-survey` skill against:

$ARGUMENTS

Return the cluster map, dominant pattern, outliers, and a convergence recommendation. Cite official docs for the libraries involved.
